@model IEnumerable<CarrierIntegrator.Tsession>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_sesion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.nombre_usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_sesion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.nombre_usuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_session }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_session }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_session })
        </td>
    </tr>
}

</table>
